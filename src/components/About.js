import React from 'react'

const About = () => {
    return (
        <div className="container">
            <h4 className="text-center">About</h4>
            <p>Patient Tracker is a system for registering patients in a hospital. Attendants are able to book patient appointments so as to manage their time efficiently.</p>
        </div>
    )
}

export default About;